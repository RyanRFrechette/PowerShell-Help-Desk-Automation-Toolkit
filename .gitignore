# Windows system files
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# Temporary files
*.tmp
*.temp
~$*

# PowerShell temp outputs (raw, unreviewed)
*.log

# macOS (if ever opened on Mac)
.DS_Store

# Editor directories
.vscode/
.idea/

# Keep outputs/reports/ tracked (example output files belong in the repo)
# Keep screenshots/ tracked (proof screenshots belong in the repo)
